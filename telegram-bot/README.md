# Telegram Бот для игры "Быки и Коровы"

## Описание

Этот Telegram бот интегрируется с игрой "Быки и Коровы" и предоставляет следующий функционал:

- 🎮 Запуск игры через WebApp
- 👥 Просмотр онлайн участников
- 📨 Система приглашений игроков
- 🔔 Уведомления о приглашениях
- 🔍 Поиск игроков по логину

## Установка

### 1. Установите зависимости

```bash
cd telegram-bot
pip install -r requirements.txt
```

### 2. Настройте переменные окружения

Скопируйте `.env.example` в `.env` и заполните необходимые данные:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
BOT_TOKEN=8131071089:AAEf_oNUIDV-HGYzptZ5ZAiWSHyriA9co3s
WEBAPP_URL=https://your-webapp-url.com
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
```

### 3. Примените миграции базы данных

Выполните SQL миграцию `supabase-migration-invitations.sql` в вашей Supabase консоли:

```sql
-- Скопируйте и выполните содержимое файла supabase-migration-invitations.sql
```

## Запуск

Для полноценной работы нужно запустить 2 процесса:

### 1. Основной бот (обработка команд)
```bash
python bot.py
```

### 2. Слушатель приглашений (отправка уведомлений)
```bash
python invitations_listener.py
```

Или запустите оба процесса одновременно:
```bash
# Linux/Mac
python bot.py & python invitations_listener.py

# Или используйте screen/tmux
screen -dmS bot python bot.py
screen -dmS listener python invitations_listener.py
```

## Команды бота

- `/start` - Главное меню
- `/participants` - Список участников
- `/help` - Помощь

## Функционал

### 1. Главное меню

При запуске `/start` бот показывает главное меню с кнопками:
- 🎮 **Играть** - открывает WebApp с игрой
- 👥 **Участники** - список онлайн игроков
- 📨 **Мои приглашения** - активные приглашения
- ℹ️ **Помощь** - справка по игре

### 2. Просмотр участников

Показывает список онлайн игроков с возможностью:
- Просмотр до 50 онлайн участников
- Быстрые кнопки для приглашения (первые 10 игроков)
- Статус онлайн (🟢 - онлайн)

### 3. Система приглашений

**Отправка приглашения:**
- Через кнопки в списке участников
- Автоматическая отправка уведомления получателю

**Получение приглашения:**
- Уведомление в боте
- Просмотр в разделе "Мои приглашения"
- Кнопки "Принять" / "Отклонить"

### 4. Интеграция с WebApp

Бот автоматически регистрирует пользователей в базе данных при первом запуске и синхронизирует данные Telegram:
- ID пользователя
- Username
- Имя и фамилия
- Статус онлайн

## Структура базы данных

Бот использует следующие таблицы:

### `players`
Расширена полями для Telegram:
- `telegram_id` - Telegram User ID
- `telegram_username` - username
- `telegram_first_name` - имя
- `telegram_last_name` - фамилия

### `games`
Добавлено поле:
- `game_name` - название игры для поиска

### `invitations`
Новая таблица для приглашений:
- `id` - UUID приглашения
- `game_id` - ID игры
- `from_player_id` - отправитель
- `to_player_id` - получатель
- `status` - статус (PENDING, ACCEPTED, REJECTED, EXPIRED)

## Развертывание

### На сервере

1. Установите Python 3.9+
2. Склонируйте репозиторий
3. Установите зависимости
4. Настройте `.env`
5. Запустите бота:

```bash
nohup python bot.py > bot.log 2>&1 &
```

### С использованием systemd

Создайте файл `/etc/systemd/system/igra-bot.service`:

```ini
[Unit]
Description=Igra Telegram Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/igra/telegram-bot
ExecStart=/usr/bin/python3 bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:

```bash
sudo systemctl enable igra-bot
sudo systemctl start igra-bot
```

## Автоматическая очистка

Для автоматической очистки старых приглашений добавьте в cron:

```bash
# Очистка приглашений каждый час
0 * * * * psql -h your-db-host -U your-user -d your-db -c "SELECT cleanup_expired_invitations();"
```

## Поддержка

При возникновении проблем проверьте:
1. Правильность токена бота
2. Доступность Supabase
3. Корректность переменных окружения
4. Логи бота в `bot.log`

## Лицензия

MIT
